<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.uniso.lpdm.lemonpepper">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LogoActivity"></activity>
        <activity android:name=".CronometroActivity" />
        <activity android:name=".ExibirProdutoActivity" />
        <activity android:name=".NovoProdutoActivity" />
        <activity android:name=".CriarEmailActivity" />
        <activity android:name=".MensagemCozinhaActivity" />
        <activity android:name=".EstoqueActivity" />
        <activity android:name=".LemonPepperActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>